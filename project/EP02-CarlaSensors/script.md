>>> 开场 #B01
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的扁平化技术风格教程封面图。深色背景纯色 #0d1117。

主体居中布局：
- 顶部小字标签 "CARLA × SparseDriveV2 仿真教程"，灰色 #8b949e。
- 主标题 "EP02: 传感器与数据采集"，白色 #e6edf3 粗体超大字号，居中。
- 主标题正下方一条亮蓝色 #58a6ff 短横线，长度约 240 px。
- 副标题 "给 Ego 车装上眼睛、耳朵和触觉"，灰色 #8b949e 中等字号。

画面左下角一辆简约 Ego 车线条图标（白色细线条），旁边一行小字 "已创建 ✓" 绿色 #3fb950；车周围环绕六个相机简约图标（小三角扇形），亮蓝色 #58a6ff，下方标注 "本集任务：安装传感器"。

整体风格极简、留白克制、文字清晰可读。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
欢迎来到第二集
上一集我们创建了 Ego Vehicle
让它能在仿真里跑起来
这一集给它装上传感器
让它能像真实自动驾驶汽车一样感知周围环境

>>> 传感器全景 #B02
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的传感器布局示意图，扁平化等距俯视风格。深色背景 #0d1117。

顶部居中标题 "传感器全景"，白色 #e6edf3 粗体大字。

画面中央偏左是一辆 Ego 车俯视图（简约线条风格，约占画布宽 25%，车头朝上）。
车周围用细线箭头标注三组传感器位置：
- 车身周围六个扇形 FOV "6× RGB Camera"，亮蓝色 #58a6ff
- 车顶一个圆形扫描区 "LiDAR"，绿色 #3fb950
- 车内一个十字图标 "IMU + GNSS"，橙色 #f0883e

画面右侧是分类标签三行（每行一个分类 + 子项列表），纵向排列：
- 相机族（亮蓝色标签）：RGB / Depth / Semantic Seg / Instance Seg / DVS / Optical Flow
- 距离传感器（绿色标签）：LiDAR / Semantic LiDAR / RADAR
- 定位与事件（橙色标签）：GNSS / IMU / Collision / Lane Invasion

画面底部居中一个胶囊形提示条，深色背景 #161b22，圆角 8 px，等宽字体（monospace）：
"统一工作流：spawn(blueprint, transform, attach_to=ego) → listen(callback)"

整体风格干净极简，文字按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。⚠️ 本图包含代码区域：代码文字将由视频引擎在后期叠加渲染，本图只需提供深色代码区域背景（纯色 #161b22，圆角）和标题。不要在图片中生成具体的代码字符。
--- narration ---
CARLA 的传感器分为三大类
相机族种类最丰富
从 RGB 到语义分割共六种
距离传感器包括 LiDAR 和 RADAR
还有 GNSS、IMU 和事件检测器
所有传感器遵循同一套工作流
创建 Blueprint、挂载到 Ego 车、注册 listen 回调
其中 6 个 RGB 环视相机是 SparseDriveV2 的唯一输入

>>> RGB 相机：算法的眼睛 #B03
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的相机布局示意图，扁平化等距俯视风格。深色背景 #0d1117。

顶部居中标题 "RGB Camera — 算法的眼睛"，白色 #e6edf3 粗体大字。

画面主体分两部分：

左侧（占画布宽 60%）：一辆 Ego 车俯视图位于中心，六个扇形 FOV 从车身六个方向延伸出去，扇形角度约 70°，相邻扇形略有重叠，共同覆盖 360°。每个扇形用不同纯色填充（半透明）并标注名称：
- FRONT（亮蓝色 #58a6ff）正前方
- FRONT_LEFT（绿色 #3fb950）左前
- FRONT_RIGHT（橙色 #f0883e）右前
- BACK（紫色 #a371f7）正后方
- BACK_LEFT（金色 #d29922）左后
- BACK_RIGHT（红色 #f85149）右后

右侧（占画布宽 35%）一个参数面板，深色背景 #161b22 圆角 12 px，等宽字体：
- 顶部 "sensor.camera.rgb" 亮蓝色 #58a6ff
- 下方四行参数（白色 #e6edf3）：
  - image_size_x: 1600
  - image_size_y: 900
  - fov: 70
  - sensor_tick: 0.1

画面底部居中一个胶囊形提示条，深色背景 #161b22 圆角 8 px，绿色 #3fb950 文字：
"SparseDriveV2 纯视觉方案：6 个 RGB 相机 = 全部输入，无 LiDAR"

整体风格极简，扇形 FOV 半透明覆盖清晰可辨。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
RGB Camera 是最重要的传感器
SparseDriveV2 的六个环视相机就是这个类型
前方、前左、前右、后方、后左、后右
六个相机完整覆盖 360 度
可以配置分辨率、视场角和采样频率
SparseDriveV2 是纯视觉方案
六个 RGB 相机就是全部输入，不需要 LiDAR

>>> 六相机实拍：CAM_FRONT #B04
@enter: fade
@exit: fade
@visual: video(./assets/CAM_FRONT.mp4)

--- visual ---
（本块使用本地视频 ./assets/CAM_FRONT.mp4，无需生成图片）

CARLA 中 Ego 车前视相机 CAM_FRONT 的原始 RGB 输出，未经任何后处理。
分辨率 960×540，10 fps。

--- narration ---
来看 CARLA 中前视相机 CAM_FRONT 的实际输出
这是 CARLA 直接渲染的原始数据
没有经过任何后处理
这就是 SparseDriveV2 看到的画面之一

>>> 六相机实拍：CAM_FRONT_LEFT #B05
@enter: fade
@exit: fade
@visual: video(./assets/CAM_FRONT_LEFT.mp4)

--- visual ---
（本块使用本地视频 ./assets/CAM_FRONT_LEFT.mp4，无需生成图片）

CARLA 中 Ego 车左前相机 CAM_FRONT_LEFT 的原始 RGB 输出。

--- narration ---
左前相机覆盖车辆前方偏左的视野
和前视相机的视野有一定重叠
这是 SparseDriveV2 感知左侧车道的关键输入

>>> 六相机实拍：CAM_FRONT_RIGHT #B06
@enter: fade
@exit: fade
@visual: video(./assets/CAM_FRONT_RIGHT.mp4)

--- visual ---
（本块使用本地视频 ./assets/CAM_FRONT_RIGHT.mp4，无需生成图片）

CARLA 中 Ego 车右前相机 CAM_FRONT_RIGHT 的原始 RGB 输出。

--- narration ---
右前相机覆盖右侧前方
对感知 cut-in 和右侧来车非常重要
前左、前右与正前三个相机组成 SparseDriveV2 规划的主要视野

>>> 深度与语义分割相机 #B07
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的四宫格对比图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "Depth & Semantic Segmentation"，白色 #e6edf3 粗体大字。

画面中央是一个 2×2 网格，总宽占画布 75%，每格之间用细灰线 #30363d 分隔。每格内部都是同一个驾驶场景的不同视图（场景内容：一条城市道路、远方两辆车、左右人行道和天空）：

左上格 "RGB 原始"：自然彩色场景，蓝天、灰路面、红黄车辆、绿植被。
右上格 "Depth"：同样场景但用灰度渐变表示距离（近处亮白 → 远处深黑），下方一行小字 "近→远，三通道编码距离"。
左下格 "Semantic Seg"：同样场景但每个像素被替换为类别色块——道路紫色、车辆红色、植被绿色、天空白色、行人黄色，下方小字 "28 个类别"。
右下格 "Instance Seg"：同样场景但两辆车用不同深浅的红色区分（实例 ID），下方小字 "类别 + 实例 ID"。

每格左上角有格子标题（白色粗体）。

画面底部居中一个胶囊形提示条，深色背景 #161b22 圆角 8 px，金色 #d29922 文字：
"训练阶段使用 · 提供完美像素级 Ground Truth"

整体风格干净、信息密度适中。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
除了 RGB，CARLA 还提供深度和分割相机
Depth 输出每个像素到相机的距离
语义分割给每个像素打上 28 个类别标签
实例分割再区分同类的不同个体
这些数据在训练阶段非常重要
提供完美的像素级 Ground Truth
监督感知模型的训练

>>> LiDAR 与 RADAR #B08
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的两栏对比图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "LiDAR & RADAR"，白色 #e6edf3 粗体大字。

画面分左右两栏，每栏一张大卡片，圆角 16 px、深色背景 #161b22、1 px 灰边 #30363d、内边距大。

左栏 "LiDAR — 激光雷达"：
- 卡片上半部一个示意图：中心一个圆形 LiDAR 单元（亮蓝色 #58a6ff），向四周辐射多条射线，射线末端散布密集的绿色小点（点云），整体呈圆形扫描场。
- 下半部参数列表（中等字号，行间距充足，白色 #e6edf3）：
  - channels: 32 / 64
  - range: 100 m · rotation: 10 Hz
  - points_per_second: 600K
- 卡片底部一行绿色 #3fb950 小字 "Semantic LiDAR 变体：点云 + 语义标签"。

右栏 "RADAR — 毫米波雷达"：
- 上半部示意图：一个橙色 #f0883e 半透明锥形扇区从中心点向外扩散。
- 下半部参数：
  - 锥形探测区域
  - 返回：速度 + 方位角 + 深度
  - 探测距离可配置

画面底部居中一行灰色 #8b949e 中等字号文字：
"SparseDriveV2 不使用 LiDAR/RADAR，但它们是其他方案的标配"

整体风格干净、对称。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
LiDAR 通过旋转扫描生成三维点云
可以配置通道数、探测距离和旋转频率
RADAR 用锥形区域探测，返回速度和方位角
这两种传感器在很多方案中都是标配
但 SparseDriveV2 选择了纯视觉路线，不依赖它们

>>> 定位与事件检测 #B09
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的双行信息图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "定位与事件检测传感器"，白色 #e6edf3 粗体大字。

画面分上下两行：

上行 "定位传感器"，三个并排卡片，总宽占画布 80%。每张卡片圆角 12 px、深色背景 #161b22、内边距大、不同颜色边框、顶部一个简约图标：
- 卡片 1 "GNSS"，绿色 #3fb950 边框、卫星图标，下方文字 "纬度 / 经度 / 海拔 · 可配置噪声模型"
- 卡片 2 "IMU"，橙色 #f0883e 边框、加速度图标，下方 "加速度计 + 陀螺仪 · 高斯噪声"
- 卡片 3 "罗盘"，亮蓝色 #58a6ff 边框、指北针图标，下方 "朝向 · 磁偏角模拟"

下行 "事件检测器"，竖向三条记录，每条左侧一个彩色圆形图标，右侧一行说明：
- 红色 #f85149：Collision Detector — 碰撞时触发回调
- 金色 #d29922：Lane Invasion — 压线时触发
- 亮蓝色 #58a6ff：Obstacle Detector — 前方障碍物告警

画面底部居中一个胶囊形提示条，深色背景 #161b22 圆角 8 px，绿色 #3fb950 文字：
"事件检测器 → Bench2Drive 扣分项的直接来源"

整体风格干净，文字按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
GNSS 和 IMU 提供定位和姿态信息
可以加入噪声来模拟真实传感器的误差
三种事件检测器在闭环评测中至关重要
碰撞、压线、闯红灯的每次违规
都通过它们记录并计入最终的 Driving Score

>>> Ground Truth：训练与推理的区别 #B10
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的左右分屏对比图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "Ground Truth 的使用边界"，白色 #e6edf3 粗体大字。

画面中央左右两个等宽矩形区域紧贴，中间用一条红色 #f85149 竖线 2 px 分隔，竖线顶部居中一行红色小字 "不能偷看！"。

左半 "训练阶段"，圆角 12 px、深色背景 #0d1117、2 px 绿色 #3fb950 边框、内边距大：
- 顶部一个驾驶场景示意图（俯视 Ego 车 + 前方道路 + 两辆 NPC 车），图像上叠加了绿色虚线 3D 包围盒标注框（包住每辆 NPC）、半透明语义分割色层覆盖路面。
- 下方标签 "监督信号"，绿色 #3fb950 粗体中字。
- 再下方三行（白色 #e6edf3）：
  - GT 包围盒 → L_det
  - GT 语义图 → L_map
  - GT 轨迹 → L_plan
- 卡片底部一行灰色 #8b949e 小字 "模型学习：正确结果是什么"。

右半 "推理 / 闭环评测"，同样布局、2 px 橙色 #f0883e 边框：
- 顶部同一场景，但只有原始相机视图，无任何标注叠加，画面干净。
- 标签 "输入"，橙色 #f0883e 粗体中字。
- 三行（白色 #e6edf3）：
  - 仅原始传感器数据
  - 6 × RGB 图像
  - 无 GT、无标注、无地图
- 卡片底部一行灰色 #8b949e 小字 "模型推断：自己判断一切"。

画面底部居中一个胶囊形提示条，深色背景 #161b22 圆角 8 px，白色 #e6edf3 中等字号粗体：
"GT 是训练时的老师，不是考试时的答案"

整体风格强对比、文字清晰，左右两侧场景必须是同一构图。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
一个非常重要的区分
训练阶段
**Ground Truth** 是监督信号
包围盒、语义图、轨迹
模型用它们学习"正确结果"
但到了推理和闭环评测阶段
模型只能看原始传感器数据
6 张 RGB 图像
不能偷看 GT，不能偷看高精地图
GT 是训练时的老师
不是考试时的答案

>>> 六相机数据流：从仿真到算法 #B11
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的数据流管线图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "六相机数据流"，白色 #e6edf3 粗体大字；下方一行副标题 "从 CARLA 仿真到 SparseDriveV2 输入"，亮蓝色 #58a6ff。

画面主体是从左到右的水平管线，分四个阶段，用带箭头的细横线连接，总宽占画布 92%：

阶段 1（最左）：Ego 车俯视图 + 周围六个扇形 FOV 同时亮起（六色扇形，分别对应六个相机方向）。下方一行小字 "tick 同步采集"。

阶段 2：六个小图像矩形按 2×3 排列，每个 60×40 px，用不同相机的代表色作边框（蓝/绿/橙/紫/金/红）。每个矩形左上角同样的小字 "frame_id = N"。下方绿色 #3fb950 小字 "同一帧 · 同一 timestamp"。

阶段 3：一摞叠在一起的图像卡片（视觉上像一叠堆栈，约 6 张），标签 "Image Queue (FIFO)"，下方等宽字体 "6 × H × W × 3"。

阶段 4（最右）：一个矩形大卡片 "SparseDriveV2 Model"，圆角 12 px、深色背景 #161b22、2 px 绿色 #3fb950 边框。内部一行 "Forward Pass → Trajectory"，下方灰色小字 "唯一的传感器输入"。

四个阶段之间各有一个右指箭头 + 上方小字标签：tick → 同步 → stack & normalize

画面底部居中一行亮蓝色 #58a6ff 中等字号粗体：
"纯视觉端到端：6 个 RGB 图像进，1 条轨迹出"

整体风格干净、节点对齐整齐、箭头清晰。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
来看这 6 个相机如何把数据喂给算法
每一帧
CARLA 同步采集 6 个相机的图像
所有图像带有相同的 frame ID 和 timestamp
进入一个 FIFO 队列
按帧组织
然后 stacking 并归一化
送入 SparseDriveV2 模型
6 个 RGB 图像进，1 条规划轨迹出
这就是纯视觉端到端的完整数据流

>>> 本集总结 #B12
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的三角关系图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "EP02 小结 · 三要素明确"，白色 #e6edf3 粗体大字。

画面中央是一个等边三角形结构，三个顶点是三个大圆角矩形节点，三条连线是亮蓝色 #58a6ff 2 px 线，每条线中央带一个右指小箭头。三角形中心一个亮蓝色文字 "闭环" 粗体中字。

三个节点（圆角 12 px，内边距大，深色背景 #161b22）：

上方顶点 "CARLA"，亮蓝色 #58a6ff 标题、2 px 同色边框；下方说明 "闭环仿真环境 · 提供世界 + 传感器数据"。

左下顶点 "SparseDriveV2"，绿色 #3fb950 标题与边框；说明 "参加考试的 Agent · 6 相机 → 轨迹"。

右下顶点 "Bench2Drive"，橙色 #f0883e 标题与边框；说明 "考试协议 · 220 路线 + DS 评分"。

画面底部居中一行下集预告，亮蓝色 #58a6ff 中等字号：
"下一集：仿真引擎与 API — 让 tick 循环跑起来"

整体风格干净、对称、三角节点对齐严格。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
这一集我们给 Ego 车装上了传感器
6 个 RGB 环视相机是 SparseDriveV2 的全部输入
深度、语义分割等传感器提供训练用的 GT 监督
但要记住
推理时模型只能看原始传感器数据
这三者的关系要记牢
CARLA 是仿真环境
SparseDriveV2 是考试 Agent
Bench2Drive 是评分考试协议
下一集我们深入仿真引擎和 Python API
让 tick 循环真正跑起来
