>>> 开场 #B01
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的扁平化教程封面图。深色背景 #0d1117。

主体居中布局：
- 顶部小字标签 "CARLA × SparseDriveV2 仿真教程"，灰色 #8b949e。
- 主标题 "EP03: 仿真引擎与 API"，白色 #e6edf3 粗体大字号。
- 主标题下方亮蓝色 #58a6ff 短横线。
- 副标题 "让 tick 循环跑起来，把数据喂给算法"，灰色 #8b949e 中等字号。

画面左下角延续 EP02：一辆简约 Ego 车线条图标 + 围绕车身的六个相机扇形图标，旁标注 "已安装 ✓" 绿色 #3fb950。
画面右下角一个亮蓝色 #58a6ff 简约循环箭头图标（圆环 + 箭头头部），下方一行小字 "本集：同步控制循环"。

整体风格极简、留白克制。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
欢迎来到第三集
前两集我们创建了 Ego 车
装上了传感器
这一集我们解决一个问题
让整个系统有节奏地运转起来
CARLA 的同步模式和 Python API
是实际跑实验最核心的知识

>>> 异步 vs 同步模式 #B02
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的左右对比时序图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "异步 vs 同步模式"，白色 #e6edf3 粗体大字。

画面分左右两栏，总宽占画布 88%，中间用灰色细竖线 #30363d 分隔。

左栏 "异步模式（默认）"：
- 标题橙色 #f0883e 粗体中字。
- 一个简化时序图：两条水平细线（轨道），上面一条标 "Server" 亮蓝色 #58a6ff，下面一条标 "Client" 绿色 #3fb950。
- Server 轨道上是密集等间距的小方块（仿真步），Client 轨道上的方块与 Server 不对齐、间距不规则。
- 下方说明两行（白色 #e6edf3 中等字号）：
  - Server 全速跑，Client 随时读
  - 时间步不固定，不保证确定性

右栏 "同步模式（推荐）"：
- 标题绿色 #3fb950 粗体中字。
- 同样时序图：Server 与 Client 轨道方块一一对齐，每对方块之间有一个从 Client 指向 Server 的小箭头标注 "tick()"。
- 下方说明：
  - Client 每 tick 推一步
  - 固定时间步，完全确定性

画面底部居中一个亮蓝色 #58a6ff 实心胶囊横幅，圆角 8 px、深色 #0d1117 粗体文字：
"闭环评测 + 可复现实验 → 同步模式是唯一选择"

整体风格干净、对比鲜明、文字清晰。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
CARLA 有两种运行模式
异步模式下 Server 全速运行
Client 随时读取
适合不要求严格对齐的快速采集
同步模式下 Client 每调用一次 tick
Server 才走一步
固定时间步，整个仿真完全确定性可复现
闭环评测和可复现实验必须用同步模式

>>> 固定时间步 #B03
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的代码 + 示意图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "固定时间步与 Sub-Stepping"，白色 #e6edf3 粗体大字。

画面左侧（占画布宽 50%）一个代码块，背景 #161b22 圆角 12 px，等宽字体（monospace），语法高亮：

```python
settings = world.get_settings()
settings.synchronous_mode = True
settings.fixed_delta_seconds = 0.05
world.apply_settings(settings)
```

- 关键字 `settings`, `True`, `0.05` 高亮
- 标识符 #e6edf3 普通颜色

画面右侧（占画布宽 42%）是两部分示意：

上半部一条水平时间轴：横线 #30363d，线上每 0.05 s 一个亮蓝色 #58a6ff 小圆点，共约 10 个圆点，下方一行 "20 ticks / 秒"。

下半部一个大方块代表一个 tick（宽约画布 35%），内部用 4 条灰色虚线分成 5 个子步（sub-step），下方一行小字 "物理子步 ≥ 60Hz → 每子步 ≤ 0.01666 s（碰撞 / 动力学数值稳定性要求）"，灰色 #8b949e 中等字号。

整体风格干净、代码语法准确、文字按字面渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。⚠️ 本图包含代码区域：代码文字将由视频引擎在后期叠加渲染，本图只需提供深色代码区域背景（纯色 #161b22，圆角）和标题。不要在图片中生成具体的代码字符。
--- narration ---
设置同步模式只要四行代码
fixed_delta_seconds 设为 0.05 秒
意味着仿真以 20Hz 推进
物理引擎在每个 tick 内部做 Sub-Stepping
把大步细分成多个子步
保证碰撞检测精度
子步频率建议不低于 60Hz

>>> NPC 行为：TM 与 ScenarioRunner #B04
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的双栏分工图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "NPC 行为：两个层次"，白色 #e6edf3 粗体大字；副标题 "Traffic Manager 管常态车流，ScenarioRunner 管考试场景"，亮蓝色 #58a6ff。

画面分左右两个大卡片，总宽占画布 88%，每张圆角 16 px、深色背景 #161b22。

左卡片 "Traffic Manager"，2 px 亮蓝色 #58a6ff 边框：
- 顶部标题粗体亮蓝色。
- 中部一张俯视示意：一段双向道路上散布若干 NPC 车辆，每辆车前方有一条小箭头表示路径（柔和、统一速度），代表"自由车流"。
- 下方要点列表（白色 #e6edf3，行间距充足）：
  - 运行在 Client 端，循环式控制
  - 五阶段流水线：定位 → 路径规划 → 碰撞检测 → 红绿灯判断 → PID 控制
  - 决策：跟车距离 / 超速比例 / 闯红灯概率 / 自动变道
  - 适用：背景车流、非考试 NPC

右卡片 "ScenarioRunner"，2 px 橙色 #f0883e 边框：
- 顶部标题粗体橙色。
- 中部俯视示意：一段道路上 Ego 车和 NPC 车的具体编排——NPC 从相邻车道追上 Ego 然后切入 Ego 前方，路径用粗箭头标注（明确剧本）。
- 下方要点：
  - py_trees 行为树驱动，事件式
  - 触发器 + 原子动作（WaypointFollower, ChangeActorTargetSpeed...）
  - 决策：精确编排 Cut-In / Pedestrian / Junction 等考试场景
  - 适用：Bench2Drive 44 种交互场景

整体风格对称、对比清晰、文字按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
CARLA 里 NPC 的行为分两个层次
**Traffic Manager** 运行在 Client 端
负责常态背景车流
五阶段流水线：定位 → 路径规划 → 碰撞检测 → 红绿灯判断 → PID 控制
你能配跟车距离、超速比例、闯红灯概率
**ScenarioRunner** 用 py_trees 行为树
精确编排考试场景
比如 Cut-In、行人横穿、路口左转
Bench2Drive 的 44 种交互场景就是这层驱动
两者协作 ——TM 撑起背景，ScenarioRunner 提供考题

>>> 行为树场景实战：Cut-In 鸟瞰 #B05
@enter: fade-up
@exit: fade
@visual: video(./assets/cutin_topdown.mp4)

--- visual ---
（本块使用本地视频 ./assets/cutin_topdown.mp4，无需生成图片）

CARLA 中 ScenarioRunner 行为树驱动的 Cut-In 场景鸟瞰画面。
Ego 车（蓝色）在右车道匀速行驶，NPC 车辆（红色）从左侧车道追上后切入 Ego 前方。
NPC 行为由 Cut-In 行为树驱动
核心原子动作包括 WaypointFollower + ChangeActorTargetSpeed。

--- narration ---
来看一个 ScenarioRunner 行为树驱动的实际场景
左侧红色 NPC 从相邻车道追上 Ego 车
然后切入 Ego 前方
这叫 Cut-In，是 Bench2Drive 考试中的经典场景
NPC 的动作由 py_trees 行为树编排
后面我们会看到 SparseDriveV2 如何应对这种场景

>>> Traffic Manager 配置 #B06
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的代码 + 注释对照图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "Traffic Manager 配置"，白色 #e6edf3 粗体大字。

画面分左右两栏，总宽占画布 85%：

左栏（占宽 55%）一个代码块，背景 #161b22 圆角 12 px，等宽字体语法高亮：

```python
tm = client.get_trafficmanager(8000)
tm.set_synchronous_mode(True)

tm.distance_to_leading_vehicle(v, 5.0)
tm.vehicle_percentage_speed_difference(v, -20)
tm.ignore_lights_percentage(v, 50)
tm.auto_lane_change(v, True)
```

- 关键字 / 数字高亮配色一致

右栏（占宽 38%）一组注释卡片，竖向排列，每条左侧有亮蓝色 #58a6ff 短竖条标记，右侧一行说明（中等字号 #e6edf3）：
- 5.0 → 跟车距离 5 米
- -20 → 比限速快 20%
- 50 → 50% 闯红灯率
- True → 允许自动变道

整体风格干净、代码按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。⚠️ 本图包含代码区域：代码文字将由视频引擎在后期叠加渲染，本图只需提供深色代码区域背景（纯色 #161b22，圆角）和标题。不要在图片中生成具体的代码字符。
--- narration ---
Traffic Manager 可以全局设定
也可以对单辆车精确调参
跟车距离、超速比例、闯红灯概率、变道策略
全都可配
这让你能构造从温和到激进的各种背景车流
对测试算法的鲁棒性非常有用

>>> 天气与光照 #B07
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的六宫格天气与光照效果图，扁平化插画风格。深色背景 #0d1117。

顶部居中标题 "天气与光照系统"，白色 #e6edf3 粗体大字。

画面中央是一个 2×3 网格，总宽占画布 80%，每格圆角 12 px、间距适中。每格是一个抽象化的天气场景小插画（一条简化的道路 + 车 + 天空，用色调和元素体现天气）：

- 卡片 1 "ClearNoon"：明亮蓝天 #1a3a5c → #2d5a8e 渐变色调，干燥路面。
- 卡片 2 "CloudyNoon"：灰蓝天空 #263545，云层覆盖，路面干燥。
- 卡片 3 "WetNoon"：阴天湿路面，路面有镜面反光。
- 卡片 4 "HardRainNoon"：暗灰天空 #1a2332，斜向雨线纹理覆盖。
- 卡片 5 "ClearSunset"：橙红渐变 #4a2020 → #8a4020，长投影。
- 卡片 6 "Custom Fog"：均匀灰色 #3a3a3a 覆盖，远景被雾隐去，标注 "fog_density / fog_distance"。

每张卡片底部一行白色 #e6edf3 中等字号粗体写预设或效果名称。

网格下方居中一个胶囊形提示条，深色背景 #161b22 圆角 8 px，金色 #d29922 文字：
"天气仅影响视觉和传感器数据 — 不影响车辆物理（无打滑效果）"

整体风格扁平、色调饱和、文字清晰。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
CARLA 内置多种天气预设
覆盖晴天、阴天、湿路面、雨天和黄昏
雾效这类效果
可以通过 fog_density、fog_distance 等参数自定义
天气参数可以实时通过 API 调整
重要细节
天气变化只影响视觉和传感器
不会改变车辆物理
雨天不会打滑
Bench2Drive 在不同天气下评测
这对算法的鲁棒性是一个挑战

>>> Python API：连接与创建 #B08
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的全屏代码截图风格，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "Python API — 连接与创建"，白色 #e6edf3 粗体大字。

画面中央是一个超大代码块，宽度占画布 78%，背景 #161b22 圆角 12 px，等宽字体（monospace），完整语法高亮。代码内容按字面准确呈现（包括缩进、引号、点号、注释）：

```python
import carla

# 1. 连接 Server
client = carla.Client('localhost', 2000)
client.set_timeout(10.0)
world = client.get_world()

# 2. 同步模式
settings = world.get_settings()
settings.synchronous_mode = True
settings.fixed_delta_seconds = 0.05
world.apply_settings(settings)

# 3. 生成 Ego Vehicle
bp_lib = world.get_blueprint_library()
ego_bp = bp_lib.find('vehicle.tesla.model3')
spawn = world.get_map().get_spawn_points()[0]
ego = world.spawn_actor(ego_bp, spawn)
```

- 关键字 #ff7b72
- 字符串 #a5d6ff
- 数字 #79c0ff
- 注释 #8b949e
- 标识符 #e6edf3

每段（连接 / 同步 / 生成）左侧有一根细灰竖条标记区段。

整体风格简洁、代码必须按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。⚠️ 本图包含代码区域：代码文字将由视频引擎在后期叠加渲染，本图只需提供深色代码区域背景（纯色 #161b22，圆角）和标题。不要在图片中生成具体的代码字符。
--- narration ---
来快速回顾 Python API 的标准开头
连接 Server
设置同步模式
生成 Ego Vehicle
这是每个 CARLA 实验的前三步
接下来是这一集的关键
传感器加主循环

>>> 同步数据闭环：完整 tick 循环 #B09
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的环形流程图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "同步数据闭环"，白色 #e6edf3 粗体大字；副标题 "每帧都在这个环里运转" 亮蓝色 #58a6ff 中等字号。

画面中央是一个正圆形流程环（严格圆形，用圆规或矢量工具确保正圆），圆环直径约占画布 65%，圆环上沿圆周顺时针均匀等距排列 5 个节点矩形（各节点之间的弧间距严格相等）（圆角 12 px、深色背景 #161b22、内边距大）：

- 12 点位置（最上）"world.tick()"，2 px 亮蓝色 #58a6ff 边框、白色粗体大字。
- 顺时针 2 点位置 "Sensor Callback"，2 px 绿色 #3fb950 边框；下方小字 "6 相机 + 其他传感器采集数据"。
- 5 点位置 "Data Align & Queue"，2 px 橙色 #f0883e 边框；下方小字 "同一 frame_id 对齐 · 按帧组织"。
- 7 点位置 "Model Inference"，2 px 紫色 #a371f7 边框；下方小字 "SparseDriveV2 Forward · 感知 → 规划 → 轨迹"。
- 10 点位置 "apply_control()"，2 px 金色 #d29922 边框；下方小字 "VehicleControl(throttle, steer, brake) · Pure Pursuit 轨迹跟踪"。

节点之间用顺时针弧形细线连接，每段弧线中央有一个右指（沿弧切线方向）的小箭头。

环形中心一行金色 #d29922 中等字号粗体文字（两行）：
"tick 间隔 50 ms"
"整个环必须在 50 ms 内完成 — 推理延迟是硬约束"

整体风格干净、环形对称、文字清晰、节点对齐严格。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
这是整个系列最重要的图
**同步数据闭环**
每一帧都按这个环运转
world.tick 推进仿真一步
传感器回调采集 6 个相机和其他传感器数据
同一帧的数据按 frame ID 对齐进入队列
然后送入 SparseDriveV2 做一次完整推理
从感知、建图到规划
输出一条轨迹
最后 apply_control 把油门、转向、刹车发给 Ego
然后进入下一帧
tick 间隔 50 毫秒
整个环必须在这 50 毫秒内完成
这是 Sparse 范式追求效率的原因

>>> 闭环实战：Cut-In 完整 tick 循环 #B10
@enter: fade
@exit: fade
@visual: video(./assets/cutin_chase.mp4)

--- visual ---
（本块使用本地视频 ./assets/cutin_chase.mp4，无需生成图片）

SparseDriveV2 闭环控制下的 Cut-In 场景，第三人称跟车视角。
每一帧：world.tick() → 6 相机采集 → SparseDriveV2 推理 → Pure Pursuit 轨迹跟踪 → ego 执行。
画面中 NPC（红色）从左侧切入，ego（蓝色）检测到后主动减速让行。

--- narration ---
这就是同步数据闭环在实际运行中的样子
注意画面中的蓝色 Ego 车和红色 NPC
左侧红色车切入时
Ego 的 SparseDriveV2 检测到它
输出减速轨迹
Pure Pursuit 轨迹跟踪控制器
把这条轨迹转成刹车控制
整个过程在每帧的 50 ms tick 间隔内完成
到这一集我们有了世界、传感器和控制循环
下一集开始
我们要先理解端到端的范式选择
为什么是 Sparse 这条路线

>>> 本集总结 #B11
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的进度小结图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "EP03 小结 · 搭建完成"，白色 #e6edf3 粗体大字。

画面中央是一条横向进度条：宽度占画布 80%，高 8 px，圆角 4 px。底色 #30363d，左半段（约 50%）填充亮蓝色 #58a6ff。进度条上方一行小字 "仿真环境搭建完成 ✓"，绿色 #3fb950 中等字号。

进度条下方竖向排列四条能力清单，每条左侧一个图标 + 状态标记，字体中等字号：
- ✓ Ego Vehicle — 创建、控制（绿色 #3fb950 文字）
- ✓ 传感器系统 — 6 相机 + 辅助传感器（绿色）
- ✓ 同步控制循环 — tick → callback → queue → control（绿色）
- ○ 算法模块 — 下一阶段（灰色 #8b949e 文字 + 空心圆形标记）

清单下方居中一句桥接文案，亮蓝色 #58a6ff 中等字号：
"仿真环境就绪。下一步：把视角转向循环里的 Model Inference。"

画面底部居中一行下集预告，亮蓝色 #58a6ff 中等字号粗体：
"下一集：端到端自动驾驶范式演进 — 为什么选择 Sparse？"

整体风格干净极简、信息清晰。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
三集下来
CARLA 端的基础搭建已经完成
Ego Vehicle 创建
传感器系统安装
同步控制循环跑通
tick → callback → queue
→ inference → control
这个闭环是后续一切的基础
下一集开始
我们把视角转向这个环里最关键的一步
Model Inference 到底在做什么
端到端自动驾驶是怎么从传感器输入走到规划输出的
