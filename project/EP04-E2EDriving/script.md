>>> 开场 #B01
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的扁平化教程封面图。深色背景 #0d1117。

主体居中布局：
- 顶部标签 "CARLA × SparseDriveV2 仿真教程"，灰色 #8b949e 小字。
- 主标题 "EP04: 端到端自动驾驶范式演进"，白色 #e6edf3 粗体大字号。
- 主标题下方亮蓝色 #58a6ff 短横线（约 240 px）。
- 副标题 "为什么 Sparse？范式如何一步步演进到今天"，灰色 #8b949e 中等字号。

画面左下角是一个简化版"同步数据闭环"环形流程图（继承 EP03 #B09 的视觉），但只用细线条勾勒、缩小到画布宽 18%；环上五个节点中只有 "Model Inference" 节点用紫色 #a371f7 实心填充并发出柔和辉光，其余节点淡灰色。环旁标注一行小字 "本集聚焦：Model Inference 内部是什么？"，亮蓝色 #58a6ff。

整体风格极简、留白克制、文字清晰。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
欢迎来到第四集
前三集我们建好了 CARLA 仿真环境
装好了传感器
跑通了 tick 循环
现在最关键的问题来了
tick 循环里的 Model Inference 到底在做什么
端到端自动驾驶是怎么演进的
为什么我们最终选择 Sparse 范式 + SparseDriveV2

>>> 模块化方案的问题 #B02
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的水平流水线 + 问题列表图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "传统模块化方案"，白色 #e6edf3 粗体大字。

画面中央是一条水平五模块流水线，总宽占画布 88%，每个模块矩形（圆角 12 px、深色背景 #161b22、内边距充足、不同颜色边框、内部白色粗体中字），用细横线 + 右指箭头连接：

感知 → 跟踪 → 预测 → 规划 → 控制
（边框颜色依次：亮蓝 #58a6ff、绿色 #3fb950、橙色 #f0883e、紫色 #a371f7、金色 #d29922）

流水线下方约一行间距处是三条红色叉号 ✗（#f85149）前缀的问题列表，竖排，文字白色 #e6edf3 中等字号：

✗ 模块边界处信息被压缩为结构化接口 → 信息丢失
✗ 上游检测错误逐级传导，下游无法修正 → 误差累积
✗ 各模块独立训练，目标函数不一致 → 无法联合优化

整体风格干净，文字按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
传统方案把自动驾驶切成五个独立模块
感知、跟踪、预测、规划、控制
各自负责一块
这套方案工程上成熟
但有三个根本问题
信息在模块边界被压缩
丰富的特征只剩几个数字
上游的检测错误一路传导到规划
下游无法修正
最关键的是各模块独立训练
目标函数不一致
这些痛点催生了端到端方案

>>> 端到端的真正含义 #B03
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的端到端流程示意图，扁平化技术风格。深色背景 #0d1117。

顶部居中主标题 "端到端 ≠ 黑盒"，白色 #e6edf3 粗体大字；副标题 "训练目标统一 + 梯度全局回传 + 内部可解释"，亮蓝色 #58a6ff 中等字号。

画面中央是从左到右的流程图，总宽占画布 85%：

最左 6 个相机输入（2×3 排列的小矩形，每个不同色边框，标签 "6×RGB"）。
→ 中间一个大圆角矩形 "End-to-End Network"，圆角 16 px、深色背景 #161b22、2 px 亮蓝色 #58a6ff 边框，内部用细灰色虚线分隔为五个子任务模块横向排列：
  Detection │ Tracking │ Mapping │ Prediction │ Planning
（每个子任务文字颜色略不同：亮蓝 / 绿 / 橙 / 紫 / 金，中等字号）
底部一行灰色 #8b949e 小字 "互联但可解释的子任务"。
→ 最右输出一个绿色 #3fb950 圆角矩形 "Trajectory"。

画面下方约一行间距处有一条从最右 "Planning" 回到最左 "Detection" 的粗弧形箭头（绿色 #3fb950 4 px 厚，带柔和发光），箭头中部标签 "Gradient Flow — 规划损失梯度回传到感知层"，绿色文字中等字号。

最底部一句核心总结，居中白色 #e6edf3 中等字号粗体：
"端到端的本质：训练目标来自最终驾驶效果，但内部仍可结构清晰、逐层可解释"

整体风格干净、信息密度适中、对称。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
端到端常被误解为"一个黑盒搞定一切"
更准确的理解是
端到端的训练目标来自最终驾驶效果
梯度从规划损失一路回传到感知层
实现全局联合优化
但网络内部仍然可以有检测、建图、预测这些可解释子任务
就像 SparseDriveV2，内部结构清晰
只是所有模块共享同一个最终目标 —— 安全驾驶

>>> Dense BEV 时代 #B04
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的 Dense BEV 示意图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "Dense BEV 时代"，白色 #e6edf3 粗体大字；副标题 "代表作：UniAD (CVPR 2023 Best Paper)"，亮蓝色 #58a6ff。

画面主体是一张俯视路口场景（占画布宽 65%，居中靠左），上面有：
- 一条十字路口的简化道路（深灰色路面）
- 几辆 NPC 车辆（小色块）和 Ego 车（蓝色色块）
- 整张图被一个 12×20 的细网格（亮蓝色 #58a6ff 半透明，每格点都发出柔和小辉光）密集覆盖——表示 Dense BEV，每个格子都在被计算
- 网格边缘超出实际道路区域（包括天空、空地），强调"大量算力浪费在空白区域"

场景右侧（占画布宽 30%）一组信息：

- 上半部一个串行模块流水线（竖向 4 个小矩形）：
  Detection → Tracking → Prediction → Planning
  下方一行小字 "在 BEV 特征图上依次执行"。

- 下半部一个性能数据卡片（深色背景 #161b22 圆角 12 px、内边距大）：
  - 训练：144h · A100
  - 推理：1.8 FPS（红色 #f85149 文字）

整体风格干净，网格视觉醒目，文字按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
2023 年，UniAD 拿下 CVPR Best Paper
它是第一个真正把感知到规划串成端到端的完整系统
核心是一张 Dense BEV Feature Map
把多视角图像投影到鸟瞰图
形成密集特征网格
然后在上面依次做检测、跟踪、预测和规划
效果很好，但代价也很大
A100 上训练 144 小时
推理只有 1.8 FPS
因为每个网格点都在算
包括天空和远处的空白区域

>>> 从 Dense → Vectorized → Sparse #B05
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的三阶段对照图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "从 Dense → Vectorized → Sparse"，白色 #e6edf3 粗体大字；副标题 "同一场景，三种表示方式" 亮蓝色 #58a6ff 中等字号。

画面中央是三个并排的同一路口俯视场景小图（每个占画布宽 28%，间距充足，圆角 12 px、内边距适中）。三个场景用极度简化的俯视图：灰色 #30363d 路面 + 白色虚线车道线 + 几个彩色小矩形代表车辆。三个场景的道路布局和车辆位置必须完全一致（拷贝同一底图），唯一区别是上层覆盖物不同。这是全系列难度最高的图之一，如果无法保证三栏完全一致，分别生成三张图后拼合亦可：

左场景 "Dense BEV (UniAD)"：
- 整场景被亮蓝色 #58a6ff 半透明 12×20 网格覆盖，所有格点发光。
- 下方一行红色 #f85149 小字 "满屏计算"。

中场景 "Vectorized (VAD)"：
- 网格消失，场景中只剩几条向量元素：蓝色 #58a6ff Polyline 表示车道线、红色 #f85149 短箭头序列表示 NPC 车辆轨迹、绿色 #3fb950 折线表示规划路径。
- 下方一行金色 #d29922 小字 "紧凑向量"。
- 底部小字灰色 #8b949e "仍依赖 BEV Encoder 提取初始特征"。

右场景 "Sparse (SparseDriveV2)"：
- 场景中只有 5 个 anchor 元素：3 个蓝色 #58a6ff 3D 包围盒（贴在车辆上）+ 2 条绿色 #3fb950 polyline（贴在车道线上）。
- 每个 anchor 上方一个小标签 "特征+锚点"（白色 14 px）。
- 其余区域完全空白（背景纯色）。
- 下方一行绿色 #3fb950 小字 "只计算实体 · 零浪费"。

三个场景下方居中一行三栏对比条：

Dense：全场景建模 · 144h 训练 · 1.8 FPS （红色 #f85149）
Vectorized：向量化 · 更高效 · 仍依赖 BEV  （金色 #d29922）
Sparse：只建模实体 · v2 ~10h 训练 · 实时 （绿色 #3fb950）

每栏间用细灰竖线分隔。

整体风格强对比，三张同源场景突出"只是表示方式不同"的核心信息。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
我们用同一个路口场景看三种范式的区别
Dense BEV 把整个场景画满网格
每个点都算
包括路面上方的天空和远处空白
大量算力浪费
Vectorized 把场景压缩成几条向量
效率提升
但还是要 BEV Encoder 做初始特征提取
Sparse 只维护几个附着在目标上的 anchor 点
每个点同时持有可学习特征和显式几何锚点
空白区域不算
SparseDriveV2 在 L20 上训练只需约 10 小时
推理实时

>>> Sparse 范式的核心机制 #B06
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的核心算子示意图，扁平化技术风格。深色背景 #0d1117。⚠️ 本图包含 2.5D 等距空间示意，如几何精度无法保证，降级为「俯视 + 侧视两个视图并列」方案——俯视图展示 3D anchor 与相机位置关系，侧视图展示投影原理。优先保证信息传达的正确性，而非几何准确性。

顶部居中标题 "Sparse 的核心机制"，白色 #e6edf3 粗体大字。

画面分左右两部分，整体宽度占画布 88%：

左半（占宽 55%）是一个 2.5D 等距视角的 3D 空间示意：
- 底部一辆简约 Ego 车线条图标（俯视小图）
- 上方悬浮一个蓝色 #58a6ff 半透明 3D 包围盒（表示一个 Detection Query 的 anchor）
- 包围盒周围散布 8 个绿色 #3fb950 小球（关键采样点 keypoints），均匀分布在包围盒前后左右
- 从每个绿色小球出发，有一条亮蓝色 #58a6ff 细虚线箭头投射到画面右侧

右半（占宽 35%）是 6 个相机图像平面（2×3 排列，每个矩形不同色边框对应六个相机方向），左侧虚线箭头射到这些图像平面上对应位置，形成投影点（小色块），并有反向细箭头流回左侧包围盒的特征向量条。

画面顶部中央有一个突出的 Query 视觉条（宽约 320 px、高 36 px、横向分两半）：
- 左半亮蓝色 #58a6ff 实心条，标注 "Feature F"，小字 "可学习特征向量"
- 右半绿色 #3fb950 实心条，标注 "Anchor B"，小字 "3D 包围盒 (11 维)"

底部一行核心公式（深色背景 #161b22 圆角 8 px、白色 #e6edf3 粗体中等字号居中）：
"每个实体 = Instance Feature (F) + Geometric Anchor (B)"

下方两行小字（灰色 #8b949e）：
- F = 通过 Deformable Aggregation 从多视角图像采样聚合的可学习表示
- B = 显式 3D 几何：车辆 11 维 BBox / 车道线 20 点 Polyline

整体风格干净、2.5D 等距构图清晰、文字按字面准确渲染。**注意：不要追求真实 3D 渲染，用扁平 2.5D 等距示意即可。**


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。⚠️ 本图包含精确几何示意。如几何精度无法保证，优先保证整体构图和色彩关系，精确几何标注将由后期叠加。⚠️ 本图包含数学公式。公式符号使用标准数学排版，确保累乘符号 ∏、等号、数字全部准确可读。
--- narration ---
Sparse 范式的核心机制可以这样理解
每个场景实体用一个 query 表示
query 包含两部分
可学习的特征向量和显式几何锚点
关键操作是 **Deformable Aggregation**
每个 query 在它的 3D 锚点周围生成采样关键点
投影到 6 个相机视图上
提取局部特征
聚合回 query
这个 query 就知道"我在哪里、周围有什么、该往哪走"
不需要密集 BEV
计算完全集中在有意义的实体上

>>> SparseDrive → SparseDriveV2 #B07
@enter: fade
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的演进对照图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "SparseDrive → SparseDriveV2"，白色 #e6edf3 粗体大字。

画面分上下两行，总宽占画布 85%：

上行 "SparseDrive v1 (ICRA 2025)"：
- 一条水平流程：
  [Sparse Perception]（蓝色 #58a6ff 边框矩形） → [Motion Planner]（橙色 #f0883e 边框）
- Motion Planner 内部一行 "生成 6 条候选轨迹"。
- 行右侧或底部一行标签 "开环评测 (nuScenes)"，金色 #d29922 中等字号。

两行之间一条向下的细箭头（亮蓝色 #58a6ff），箭头旁一行 "核心改变"。

下行 "SparseDriveV2 (2026)"：
- 同样水平流程：
  [Sparse Perception]（同上） → [Scoring-Based Planner]（绿色 #3fb950 边框）
- Scoring 内部一行 "对 262K 候选打分"。
- 标签 "开环 + 闭环 (CARLA / Bench2Drive)"，绿色 #3fb950。

画面右侧空白区一个大问号气泡 "？"（橙色 #f0883e 中等字号），下方一行 "为什么从生成改成打分？"

整体风格干净、对称、上下对齐。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
SparseDrive v1 在 ICRA 2025 发表
首次用全稀疏表示实现了端到端自动驾驶
但它的规划模块是生成式的
从稀疏特征直接回归 6 条候选轨迹
SparseDriveV2 做了一个根本改变
它不再生成轨迹
而是对候选池打分
候选池从 6 条扩展到 26 万条
而且第一次在 CARLA 上做了真正的闭环评测
为什么从生成改成打分

>>> 生成 vs 打分：Scoring 的动机 #B08
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的左右对比图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "生成 vs 打分"，白色 #e6edf3 粗体大字。

画面分左右两栏，总宽占画布 88%，间距充足。

左栏 "生成范式 (Generation)"，圆角 16 px、深色背景 #161b22、2 px 橙色 #f0883e 边框、内边距大：
- 顶部标题橙色粗体中字。
- 中部示意图：一个抽象神经网络（几个堆叠的圆角矩形层）直接输出一条单一曲线（橙色），底部一行 "直接回归轨迹"。
- 下方两行优点（白色 #e6edf3）：
  - 灵活 · 任意形状
- 下方三行缺点（红色 #f85149）：
  - 难优化 · 模式崩塌
  - 6 条候选 → 覆盖率不足
  - 训练不稳定

右栏 "打分范式 (Scoring)"，2 px 绿色 #3fb950 边框：
- 标题绿色粗体中字。
- 中部示意图：一个大的轨迹候选池（多条不同颜色的并排曲线，约 12-16 条），一个抽象 Scorer 网络在右侧给每条曲线打分，最右边一条被高亮为金色 #d29922 并放大，标注 "选最优"。
- 优点（白色 #e6edf3）：
  - 稳定 · 只需分类/排序
  - 覆盖率高 · 只要候选够密
- 关键挑战（金色 #d29922 文字）：
  - 如何让候选池足够密而计算量又可承受？
- 底部一条细箭头指向 "→ Factorized Vocabulary"，亮蓝色 #58a6ff。

整体风格强对比、对称、文字按字面准确渲染。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
轨迹规划有两种思路
生成式灵活
能产生任意形状的轨迹
但难优化，容易模式崩塌
6 条候选的覆盖率也不够
打分式稳定，本质是排序问题
训练简单
前提是候选池必须足够密
否则好轨迹进不了候选集就没得选
SparseDriveV2 选择打分路线
它要回答一个问题
怎么让候选池足够密
同时计算量可承受
答案就是 Factorized Vocabulary，下一集展开

>>> 本集总结 #B09
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
一张 16:9 横构图的竖向时间线收尾图，扁平化技术风格。深色背景 #0d1117。

顶部居中标题 "范式演进路线"，白色 #e6edf3 粗体大字。

画面中央偏左是一条竖直时间线（灰色 #30363d 2 px 线），线上自上而下 5 个圆形节点（直径 20 px），每个节点不同颜色，节点旁边右侧有节点标签和描述。

节点（自上而下）：
1. 灰色 #8b949e 节点 — "传统"｜模块化 Pipeline
2. 橙色 #f0883e 节点 — "2023 UniAD"｜Dense BEV 端到端 · 144 h 训练
3. 金色 #d29922 节点 — "2023 VAD"｜Vectorized · 向量化
4. 绿色 #3fb950 节点 — "2024 SparseDrive"｜全稀疏 · 并行预测+规划
5. 亮蓝色 #58a6ff 节点 — "2026 SparseDriveV2"｜Scoring 范式 · 闭环评测

每条标签：年份 + 名字粗体中字，描述中等字号 #8b949e。

时间线右侧（占画布宽 50%）是一句过渡文案 + 下集预告：

中部一行亮蓝色 #58a6ff 中等字号文字：
"下一集：深入 SparseDriveV2"
"看 Factorized Vocabulary"
"和 Coarse-to-Fine 怎么做"
（分三行写）

底部一行大字预告，亮蓝色 #58a6ff 粗体中等字号：
"EP05: SparseDriveV2 架构 — Scoring is All You Need"

整体风格干净、时间线对齐严格、文字清晰。


画面风格参考：类似 Notion / Linear 文档中的扁平化技术插图，深色主题、低饱和配色。画面干净、无噪点、无照片级写实纹理、无阴影、无渐变背景。所有中文文字必须准确渲染，不得出现乱码、缺笔或方块。文字与背景对比度 ≥ 4.5:1，确保在 1920×1080 视频帧中清晰可读。重要内容集中在画面中央 80% 安全区内。无任何 3D 写实渲染、无照片、无光晕特效。
--- narration ---
这一集我们看到了自动驾驶范式的完整演进路线
从模块化 Pipeline
到 Dense BEV 的 UniAD
到 Vectorized 的 VAD
再到全稀疏的 SparseDrive
最后 SparseDriveV2 选择了 Scoring 范式
下一集是技术含量最高的一集
我们深入拆解 SparseDriveV2
看 **Factorized Vocabulary**
和 **Coarse-to-Fine** 到底怎么做的
